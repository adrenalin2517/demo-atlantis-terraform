# Local .terraform directories
**/.terraform/

# .tfstate files
*.tfstate
*.tfstate.*

# .tfvars files that are generated automatically
*.tfvars

# Crash log files
crash.log

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore files that are generated by Terraform itself
terraform.tfstate
terraform.tfstate.backup

# Ignore the .terraformrc file as it contains sensitive information
.terraformrc

# AWS SSO cache directory
.aws-sso/
